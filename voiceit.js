const ENDPOINT = YOUR_VOICEIT_ENDPOINT;
const TOKEN = YOUR_VOICEIT_TOKEN_ENDPOINT;
const AUTH0_URL = YOUR_AUTH0_DOMAIN;

let VoiceItHelper = function(options) {
  // Setup

  this.getToken = function() {
    // Get token from the voiceit_token endpoint
  };

  this.verify = function() {
    // Enroll or verify user
  };

  this.start2FAProcess = function() {
    // Get a token then verify/enroll user
  };
};
